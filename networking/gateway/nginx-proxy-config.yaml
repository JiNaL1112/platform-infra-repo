apiVersion: gateway.nginx.org/v1alpha2   # was v1alpha1
kind: NginxProxy
metadata:
  name: nginx-proxy-config
  namespace: nginx-gateway
spec:
  disableHTTP2: false   # was "http2: true" — this field doesn't exist